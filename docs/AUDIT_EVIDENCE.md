# Backend and reliability audit evidence

This audit traces the assessment from baseline `651293d711668300414ae7e228c28d29646a5096` through the reliability follow-ups. “Deterministic repository proof” means source, automated tests, config evaluation, export evidence, disposable generated-native inspection, or fixture-backed Android compilation that runs without provider credentials. It does not mean real SDK network startup, signing/domain association, provider delivery, physical-device behavior, or store-mediated attribution.

## Independent review resolution matrix

| ID | Reviewed defect | Resolution | Deterministic proof | Status |
| --- | --- | --- | --- | --- |
| 1 | Link intake and signup could race across separate queues | All journey-mutating callback/start/complete work shares one serialization boundary; a competing link cannot replace or route after freeze. | Controlled link-versus-freeze interleaving | Repository proof |
| 2 | Accepted signup could be relabeled failed by analytics, UUID/outbox, timeout, or cleanup | Backend receipt is the commit point. Completion telemetry and cleanup are retryable post-commit work; ordinary failures return the same accepted receipt and never emit `referral_signup_failed`. | Accepted analytics-timeout, cleanup-failure, cumulative-stage-latency, and receipt-retry tests | Repository proof; production backend external |
| 3 | Reset could report success before a durable epoch switch, or hang on pointer load/write | Pointer publication is the durable reset commit. Failure/timeout keeps the old epoch authoritative; retry switches only after a successful write. Attempts are generation-fenced. | Pointer rejection, hung initial read, late write, same-turn overlap, and cold-reload tests | Repository proof against AsyncStorage mock |
| 4 | Branch subscribe failure skipped local outbox and route recovery | Subscription setup is isolated; startup flush and pending/accepted restoration continue and a diagnostic is attempted. | Subscribe-throws recovery test | Repository proof through adapter fake |
| 5 | Legacy unscoped local state was abandoned | Valid journey, generated code, processed set, milestones, outbox, and receipts migrate into the active epoch, then legacy keys are removed. | Baseline/extended upgrade fixture | Repository proof |
| 6 | Late old-epoch writes and seeded inactive keys could persist indefinitely | Every epoch mutation performs a post-write tombstone check; startup scavenges inactive namespaces; post-commit cleanup is bounded/non-critical. | Actual old-key-null assertion, seeded inactive reload, hung scavenger test | Repository proof |
| 7 | Visible progress could combine same-code/different-invitee journeys | Generated/shared are intentionally code-scoped referrer milestones; clicked/started/completed require the exact invitee fingerprint. | Same-code/different-fingerprint progress and cold-snapshot tests | Repository proof |
| 8 | Numeric Branch timestamps were ignored and 32-bit attribution fingerprints collided | Finite numeric/bounded-string timestamps canonicalize identically. New fingerprints use SHA-256 truncated to 128 bits; seven-character values are read-only legacy compatibility. | Real numeric callback, distinct timestamp, supplied collision, replay stability, legacy persistence tests | Repository proof; native callback delivery external |
| 9 | Corrupt processed/milestone/outbox/receipt blobs remained physically stored | Every read/validate path canonically rewrites bounded valid data or removes invalid/empty data under the same per-key lock. | Poisoned blobs inspected after read plus cleanup/write race | Repository proof |
| 10 | Crash after receipt could lose completion and Success presentation | Exact-attribution accepted outcome plus receipt restores/retries completed analytics, cleanup, and Success across three commit-to-presentation cut points. | Post-receipt/pre-analytics, post-analytics/pre-cleanup, and post-cleanup/pre-navigation restart tests | Repository proof in one JS runtime |
| 11 | A late processed-marker write could suppress the only warm-session route | Pending is durable and routed before the marker wait; an exact processed replay routes matching persisted pending without a second click event. | Delayed marker timeout, late success, warm replay test | Repository proof |
| 12 | Outbox reservation could reuse a schema-valid record with poisoned identity | Reservation matches the complete immutable event identity; conflicting records are removed and replaced before delivery/milestone acceptance. | Wrong-code/wrong-platform collision test | Repository proof |
| 13 | Reset during post-commit work could let a stale screen navigate to Success | Authoritative acceptance remains recoverable, but lifecycle cancellation is rethrown to presentation so the stale await cannot navigate. | Reset-during-analytics and reset-during-cleanup tests | Repository proof |
| 14 | Branch test keys were passed as live-only config | Development/preview pass required `apiKey`, `testApiKey`, and `enableTestEnvironment: true`; production passes a live `apiKey`, disables test mode, and omits `testApiKey`. Split variables and the legacy alias are validated. | Config unit tests and test/live CLI probes | Config-evaluation proof only |
| 15 | One outer analytics timeout could reject otherwise successful product actions | Coordinator removes the cumulative wrapper; tracker bounds individual storage/client stages and product actions treat ordinary instrumentation failure as non-blocking. | Delayed-but-individually-in-budget generate/share/start/complete test | Repository proof |
| 16 | Delayed accepted-snapshot hydration could merge after route change/unmount | Hydration uses revision, fingerprint, and disposed guards and never re-emits analytics. | Delayed A-to-B and unmount guard tests | Repository proof |
| 17 | Reset UI assumed reset could not fail and conflated commit with refresh | Invite/Success show retryable failure without clearing ledger or navigating. Durable reset failure is distinct from post-commit presentation failure. | Failure-then-retry and `onCommitted`-throws tests | Repository proof; UI-device behavior external |
| 18 | Never-settling fingerprint digest could wedge the journey queue | Attribution parsing/digest is bounded by coordinator lifecycle; reset opens a fresh queue and suppresses stale routing. | Never-settling digest/reset/fresh-callback test | Repository proof; native Crypto behavior external |
| 19 | Already-split pending B/frozen A could restore B | Journey normalization makes frozen identity authoritative and canonicalizes pending to A. | Seeded split-state migration test | Repository proof |
| 20 | Timed-out pointer R1 could overwrite successful retry R2 | A timed-out publication is invalidated; late repair is queued on the same active-pointer serialization boundary; the winning reset confirms its pointer. | R2-then-R1 same-turn release plus simulated cold reload | Repository proof against AsyncStorage mock |
| 21 | A superseded startup loader could migrate/scavenge the new winner or remove its pointer | Startup reads, migration writes, legacy deletion, and each retired-key removal re-check generation and active/durable pointer identity. Initial creation re-reads under the pointer lock before writing. | Late A scavenger versus winning C; pre-write stale initializer; cold-reload C-state preservation | Repository proof against AsyncStorage mock |
| 22 | Coordinator's cumulative reset timeout could report failure after storage had committed | Storage owns separate pointer, cleanup, and confirmation bounds. Coordinator awaits that semantic result without a second cumulative timer; UI receives explicit `committed` state. | Slow in-budget pointer plus hung cleanup through coordinator and `commitDemoReset` | Repository proof |
| 23 | A never-settling initial pointer write could permanently own the active-key queue | Reset timeout rotates/evicts the obsolete initialization lock generation so retry can initialize and publish in the same runtime. A late native write is generation-fenced and queues serialized winner repair. | Empty-storage hung initializer recovery and delayed-write eventual-winner/cold-reload tests | Repository proof; AsyncStorage has no cancellation/CAS |

## Assessment contract matrix

| Requirement | Evidence | Proof level |
| --- | --- | --- |
| Expo React Native choice justified | Pinned Expo/React Native dependencies, custom-development-build rationale, Expo Doctor, web export, generated Android/iOS structure checks, Android debug compile | Repository/config/compile proof with non-provider fixtures; provider runtime external |
| Authenticated referrer generates unique shareable link and invokes native share | Auth-required generation, stable per-epoch mock code, HTTPS enforcement for native providers, loopback-only HTTP for the local reviewer build, Branch payload adapter, `Share.share` outcome adapter | Fixture/call-shape proof; global uniqueness and device sheet external |
| Direct incoming link routes with pre-applied code | Strict Branch-shaped parser, persist-before-route coordinator, warm/cold route tests | Application proof; App/Universal Link association external |
| Deferred link survives install and first launch | Same accepted callback path, first-session metadata, pending persistence, cold restore, `demo-deferred` web fixture | Post-callback application proof only; store handoff external |
| Branch rather than Firebase Dynamic Links | Branch native adapter, test/live plugin config, packaged runtime JSON, NativeLink parameter, and generated native inspection; architecture records Dynamic Links shutdown rationale | Source/generated-config proof; provider project/runtime external |
| Exact five events with code and platform | Typed allowlist, schema validator, full-funnel test, immutable outbox identity, accepted presentation snapshot | Repository proof through adapter acceptance |
| Firebase Analytics real signature | Native adapter calls `logEvent(getAnalytics(), name, normalizeFirebaseProperties(event))`; optional booleans become Firebase-compatible `1`/`0` | Mocked call-shape and native-compile proof; DebugView/warehouse external |
| Mock backend acceptable and failure paths covered | Stable get-or-create, explicit `+fail`, canonical atomic receipt, retries/conflicts/concurrency | Repository proof for mock |
| Reliability document weighted equally | Architecture covers silent failures, timeouts, retries, duplicate callbacks, idempotency, ordering, privacy, observability, platform behavior, rollout/rollback, and limitations | Document/code reconciliation |
| Web demo is truthful | `demo-direct`/`demo-deferred` classifications and repeated explicit store-proof boundary | Repository wording + export proof |

## Verification results

| Command/check | Result |
| --- | --- |
| `npm run typecheck` | Passed. |
| `npm run lint` | Passed with zero warnings. |
| `npm test -- --runInBand` | Passed: 12 suites, 174 tests. |
| `npm test -- --runInBand --coverage` | Passed: 90.40% statements, 82.63% branches, 90.82% functions, 93.22% lines. |
| Web/default `npx expo config --type public --json` | Passed with native providers disabled. |
| Branch preview/test and production/live probes | Passed: test maps `apiKey`/`testApiKey`/enabled test mode; live omits `testApiKey` and disables test mode; legacy test alias passed; invalid live/test prefix failed as intended. |
| Platform-specific Firebase config probes | Passed: EAS Android selected JSON only; EAS iOS selected plist only; missing Android worker file failed as intended. |
| `npx expo-doctor` | Passed: 21/21 checks. |
| `npm run build:web` | Passed: Expo exported `dist` from 609 modules. |
| Windows native wrapper negative preflight | Passed: invokes the installed Expo CLI directly and rejects missing Branch/Firebase input before generation. |
| Clean fixture-backed Android Prebuild + verifier | Passed: generated Branch runtime config, verified both App Link hosts, install-referrer receiver, test-mode key metadata, Firebase package, and Gradle plugin wiring. |
| GitHub Actions `native-structure` job | Generates and verifies Android/iOS projects, compiles an Android debug APK, and asserts the artifact exists. The exact run is linked from the repository checks. |

Coverage is a gap signal, not a completeness claim. Against the supplied 22-test baseline, aggregate statements increased from 79.86% to 90.40% (+10.54 points), branches from 77.19% to 82.63% (+5.44), and functions from 63.63% to 90.82% (+27.19). The branch percentage reflects substantial reset, migration, accepted-recovery, timeout, share-outcome, presentation, and native-configuration branching; tests prioritize the concrete reviewed behaviors rather than a vanity percentage. Final coverage is: `ReferralCoordinator` 90.41% statements / 94.62% lines; `AnalyticsTracker` 93.93% / 95.40%; `referralStorage` 87.52% / 90.54%; `runReferralShare` 100% / 100%; and `commitDemoReset` 100% statements / 100% lines.

## External proof boundary

Repository tests and CI now prove config-plugin execution, generated Android/iOS structure, and Android debug compilation with non-networked, package-correct fixtures. They do not prove real SDK network startup, signing identities, Android `assetlinks.json`/Branch domain association, iOS AASA validity, real Branch dashboard routing, a physical cold/warm callback, a store-mediated app-not-installed → install → first-launch handoff, Firebase DebugView/warehouse ingestion, global referral-code uniqueness, production authentication, authoritative account creation, fraud rules, or transactional rewards. Those checks require configured provider projects controlled by the applicant or reviewing organization, matching signed builds/domains, physical devices, Play internal testing/TestFlight, and a real backend.
